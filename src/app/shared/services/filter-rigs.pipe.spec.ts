import { FilterRigsPipe } from './filter-rigs.pipe';

describe('FilterRigsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRigsPipe();
    expect(pipe).toBeTruthy();
  });
});
